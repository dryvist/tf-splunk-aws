name: Best Practices Recommender

on:
  schedule:
    - cron: "0 3 * * 2,5"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  issues: write
  pull-requests: read

jobs:
  recommend:
    uses: JacobPEvans/ai-workflows/.github/workflows/best-practices.yml@v0.14
    secrets: inherit
