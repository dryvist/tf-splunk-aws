name: Code Simplifier

on:
  schedule:
    - cron: "0 4 * * 0,3,6"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  pull-requests: write

jobs:
  simplify:
    uses: JacobPEvans/ai-workflows/.github/workflows/code-simplifier.yml@v0.13
    secrets: inherit
