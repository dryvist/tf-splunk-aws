name: Code Simplifier

on:
  schedule:
    - cron: "0 4 * * 0,3,6"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  pull-requests: write

jobs:
  simplify:
    uses: dryvist/ai-workflows/.github/workflows/cc-code-simplifier.yml@v0.16
    secrets: inherit
