name: Next Steps

on:
  schedule:
    - cron: "0 5 * * 1,4"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  issues: write
  pull-requests: write

jobs:
  analyze:
    uses: JacobPEvans/ai-workflows/.github/workflows/next-steps.yml@v0.16
    secrets: inherit
